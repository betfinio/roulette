sources:
  - name: roulette
    handler:
      graphql:
        endpoint: ${PUBLIC_ROULETTE_GRAPH_URL}
        schemaHeaders:
          Authorization: Bearer ${PUBLIC_THYME_TOKEN}
        operationHeaders:
          Authorization: Bearer ${PUBLIC_THYME_TOKEN}
 

codegen:
  scalars:
    BigInt: bigint
    Bytes: string

documents:
  - ./src/schema.graphql